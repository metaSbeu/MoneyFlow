<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoneyFlow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@color/main_bg</item>
        <item name="android:navigationBarColor">@color/main_bg</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>

    </style>

    <style name="Theme.MoneyFlow.AuthScreen" parent="Base.Theme.MoneyFlow">
        <item name="colorPrimary">@color/main_bg</item>
    </style>

    <style name="Theme.MoneyFlow.TransactionAdd" parent="Base.Theme.MoneyFlow">
        <item name="colorPrimary">@color/light_blue</item>
    </style>

    <style name="Theme.MoneyFlow" parent="Base.Theme.MoneyFlow" />
</resources>